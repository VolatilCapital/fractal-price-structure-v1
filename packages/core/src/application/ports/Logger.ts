// Re-export depuis le domain — Logger est une interface pure du domaine
export type { Logger } from "../../domain/logger/Logger.js"
export { NoopLogger, noopLogger } from "../../domain/logger/Logger.js"
